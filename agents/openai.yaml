interface:
  display_name: "niucodes image gen"
  short_description: "Cross-platform OpenAI image gen/edit helper"
  default_prompt: "Use $niucodes-image-gen as a thin API wrapper to generate or edit images with minimal prompt rewriting."
