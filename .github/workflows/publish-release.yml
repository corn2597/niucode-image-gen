name: Publish release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing Git tag to receive the release assets
        required: true
        default: v1.3.0
        type: string

permissions:
  contents: write

jobs:
  packaged-e2e:
    name: ${{ matrix.id }} release package verification
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: windows-x64
            runner: windows-latest
            archive_id: win-x64
            package_dir: niucodes-image-gen-win-x64
            build_script: build:package:win-x64
          - id: macos-arm64
            runner: macos-14
            archive_id: macos-arm64
            package_dir: niucodes-image-gen-macos-arm64
            build_script: build:package:macos-arm64
          - id: macos-x64
            runner: macos-15-intel
            archive_id: macos-x64
            package_dir: niucodes-image-gen-macos-x64
            build_script: build:package:macos-x64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run ${{ matrix.build_script }}
      - name: Unpack and test the platform release ZIP
        shell: pwsh
        run: |
          $version = (node -p "require('./package.json').version")
          $archive = "release/niucodes-image-gen-${{ matrix.archive_id }}-v$version.zip"
          $destination = Join-Path $env:RUNNER_TEMP "niucodes-image-gen release verification"
          Expand-Archive -LiteralPath $archive -DestinationPath $destination -Force
          node scripts/tests/packaged-e2e.mjs --package-root (Join-Path $destination "${{ matrix.package_dir }}")
      - name: Upload verified platform package
        uses: actions/upload-artifact@v4
        with:
          name: niucodes-image-gen-${{ matrix.id }}
          path: release/niucodes-image-gen-${{ matrix.archive_id }}-v*.zip
          if-no-files-found: error

  assemble-release:
    needs: packaged-e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: actions/download-artifact@v4
        with:
          path: verified-packages
          merge-multiple: true

      - run: npm ci
      - run: npm run assemble:release -- --input-dir verified-packages

      - name: Upload assembled release assets
        uses: actions/upload-artifact@v4
        with:
          name: niucodes-image-gen-release-assets
          path: |
            release/niucodes-image-gen-v*.zip
            release/niucodes-image-gen-macos-arm64-v*.zip
            release/niucodes-image-gen-macos-x64-v*.zip
            release/niucodes-image-gen-win-x64-v*.zip
            release/SHA256SUMS.txt
          if-no-files-found: error

  universal-windows-e2e:
    needs: assemble-release
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          name: niucodes-image-gen-release-assets
          path: release-assets
      - name: Unpack and test the final universal ZIP directly
        shell: pwsh
        run: |
          $version = (node -p "require('./package.json').version")
          $archive = "release-assets/niucodes-image-gen-v$version.zip"
          $destination = Join-Path $env:RUNNER_TEMP "niucodes-image-gen final universal E2E"
          Expand-Archive -LiteralPath $archive -DestinationPath $destination -Force
          node scripts/tests/packaged-e2e.mjs --package-root (Join-Path $destination "niucodes-image-gen")

  publish:
    needs: universal-windows-e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: niucodes-image-gen-release-assets
          path: release

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${{ inputs.tag }}"
          version="${version#v}"
          gh release upload "${{ inputs.tag }}" \
            "release/niucodes-image-gen-v${version}.zip" \
            "release/niucodes-image-gen-macos-arm64-v${version}.zip" \
            "release/niucodes-image-gen-macos-x64-v${version}.zip" \
            "release/niucodes-image-gen-win-x64-v${version}.zip" \
            release/SHA256SUMS.txt \
            --clobber

      - name: Verify uploaded release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir downloaded-release
          gh release download "${{ inputs.tag }}" \
            --dir downloaded-release \
            --pattern "niucodes-image-gen-v*.zip" \
            --pattern "niucodes-image-gen-macos-arm64-v*.zip" \
            --pattern "niucodes-image-gen-macos-x64-v*.zip" \
            --pattern "niucodes-image-gen-win-x64-v*.zip"
          cp release/SHA256SUMS.txt downloaded-release/SHA256SUMS.txt
          (cd downloaded-release && sha256sum -c SHA256SUMS.txt)
